USAN/
│
├── index.html
├── article.html
├── style.css
└── assets/
    └── images/
    body {
  font-family: system-ui, sans-serif;
  margin: 0;
  background: #f2f2f2;
  color: #111;
}

header {
  background: #1a1a1a;
  color: white;
  padding: 20px;
}

nav {
  background: #333;
  padding: 10px;
}

nav a {
  color: white;
  margin-right: 15px;
  text-decoration: none;
}

main {
  max-width: 900px;
  margin: 20px auto;
  background: white;
  padding: 20px;
}

section {
  margin-bottom: 30px;
}
